version: '3.3'
services:
  generic-api:
    container_name: generic-api
    build: 
      context: './'
      dockerfile: ./docker/api.dockerfile
    expose:
      - '3001'
    ports:
      - '3001:3001'
  


